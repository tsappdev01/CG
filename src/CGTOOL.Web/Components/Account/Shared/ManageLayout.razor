@inherits LayoutComponentBase
@layout AccountLayout

@* The account pages carry their own heading now -- Profile renders the "Manage your account"
   header with its own status chips, and the remaining pages (password, 2FA, personal data) are
   reached directly rather than through a sub-navigation. *@
@Body
